import { createTheme } from "@material-ui/core/styles";

export default createTheme({
  palette: {
    primary: {
      main: "#2557a7",
    },
    secondary: {
      main: "#fff",
    },
  },
  typography: {
    h6: {
      fontSize: 14,
    },
    h5: {
      fontWeight: "bold",
    },
  },
});
